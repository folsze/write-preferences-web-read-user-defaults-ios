import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'write-preferences-web-read-user-defaults-ios',
  webDir: 'www'
};

export default config;
